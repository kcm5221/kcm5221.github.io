# TODO List

---

## ✅ 유지보수 체크리스트
- 데이터 피드 이상 징후 발생 시 `data/current.json`의 SHA와 `feed/page-1@<sha>.json` 일관성을 우선 확인합니다.
- OAuth/JWT 오류가 보고되면 Worker의 `CORS_ALLOW_ORIGIN` 및 허용 로그인 목록(`ALLOWED_GITHUB_LOGIN`)을 점검합니다.
- 커밋 실패 시 GitHub App 토큰 발급 흐름과 레이트리밋(`RL_MAX_REQ`) 로그를 확인하고 필요 시 값만 미세 조정합니다.
- UI/라우팅 문제는 `#/`, `#/search`, `#/profile`, `#/write`, `#/post/:slug` 경로 동작 여부를 우선 확인합니다.

## 🔄 정기 점검
- 분기별로 `npm install && npm run build`를 실행해 의존성 호환성을 확인하고, 필요한 최소 범위에서만 버전 갱신을 진행합니다.
- Cloudflare Worker와 GitHub Actions 시크릿이 최신 값과 일치하는지 확인하고, 변경 시 `manual/environment.md`를 즉시 업데이트합니다.
- Pages 배포본이 최신 JSON 피드(시간/sha)를 읽고 있는지 홈/검색 화면에서 수동 검증합니다.

## 🧭 변경 가이드
- 새로운 기능/구조 변경 요청은 수용하지 않으며, **버그 수정·보안 패치·문서 보완**만 처리합니다.
- 데이터 스키마나 해시 라우터 경로 변경은 금지하고, UI 수정도 기존 동작을 유지하는 범위에서만 수행합니다.
